__pycache__/
*.py[cod]
# Ignore everything in the weather directory
data/raw/weather/*

# But do not ignore the .dvc file
!data/raw/weather/weather.dvc

# Ignore everything in the weather directory
data/raw/bike/*

# But do not ignore the .dvc file
!data/raw/bike.dvc

/data
venv
